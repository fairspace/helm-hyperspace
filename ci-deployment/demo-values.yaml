hyperspace:
  keycloak:
    realm: demo
  ingress:
    domain: hyperspace.demo.fairway.app
    tls:
      certificate:
        dns01Provider: gke
  configurationScripts:
    dockerRepository: eu.gcr.io/fairspace-207108
    image: keycloak-configuration

# Set password to prevent having a new password at every upgrade
keycloak:
  keycloak:
    password: keycloak-demo-123
  postgresql:
    postgresPassword: "mf5-Y5Q5E5_P33eB"

