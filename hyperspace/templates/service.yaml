apiVersion: v1
kind: Service
metadata:
  name: {{ template "helm-hyperspace.fullname" . }}
  labels:
    app: {{ template "helm-hyperspace.name" . }}
    chart: {{ template "helm-hyperspace.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "helm-hyperspace.name" . }}
    release: {{ .Release.Name }}
